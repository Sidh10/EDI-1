"""Label-noise engine (Contribution 2 support).

Phase 0 ships only ``pc_foster`` in its *spike* form — enough to test Assumption
A4 (can Pc be recomputed from the public CDM fields?) on ~100 sampled CDMs. The
full ``rescale`` grid and cross-validation against a reference implementation are
built in Phase 4 (E14), governed by the Phase-0 spike outcome.
"""
